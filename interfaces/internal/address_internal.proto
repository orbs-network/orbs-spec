syntax = "proto3";

message AddressData {
    uint32 virtual_chain = 1;
    bytes address_token = 2;
    ApprovalLevel approval_level = 3;
}

enum ApprovalLevel {
    INVALID = 0;
    VALID_LEVEL1 = 1;
}